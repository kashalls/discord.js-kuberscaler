---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: discordgateways.discord.nerdz.io
spec:
  group: discord.nerdz.io
  names:
    kind: DiscordGateway
    listKind: DiscordGatewayList
    plural: discordgateways
    singular: discordgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.sharding.mode
      name: Mode
      type: string
    - jsonPath: .status.appliedShards
      name: Applied Shards
      type: integer
    - jsonPath: .status.recommendedShards
      name: Recommended
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DiscordGateway is the Schema for the discordgateways API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiscordGatewaySpec defines the desired state of DiscordGateway.
            properties:
              image:
                description: Image is the container image for shard pods
                type: string
              intents:
                description: Intents defines the Discord intents configuration
                properties:
                  privileged:
                    description: Privileged indicates whether privileged intents are
                      enabled
                    type: boolean
                type: object
              podTemplate:
                description: PodTemplate defines the pod template configuration
                properties:
                  resources:
                    description: Resources defines the resource requirements for shard
                      pods
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              sharding:
                description: Sharding defines the sharding configuration
                properties:
                  fixedShardCount:
                    description: FixedShardCount is the number of shards when mode
                      is Fixed
                    format: int32
                    type: integer
                  maxShards:
                    description: MaxShards is the maximum number of shards (used with
                      Recommended mode)
                    format: int32
                    minimum: 1
                    type: integer
                  minShards:
                    description: MinShards is the minimum number of shards (used with
                      Recommended mode)
                    format: int32
                    minimum: 1
                    type: integer
                  mode:
                    default: Recommended
                    description: Mode determines how shard count is calculated (Recommended
                      or Fixed)
                    enum:
                    - Recommended
                    - Fixed
                    type: string
                type: object
              tokenSecretRef:
                description: TokenSecretRef references the Secret containing the bot
                  token
                properties:
                  key:
                    description: Key is the key within the Secret containing the bot
                      token
                    type: string
                  name:
                    description: Name is the name of the Secret
                    type: string
                required:
                - key
                - name
                type: object
            required:
            - image
            - tokenSecretRef
            type: object
          status:
            description: DiscordGatewayStatus defines the observed state of DiscordGateway.
            properties:
              appliedShards:
                description: AppliedShards is the actual number of shards currently
                  deployed
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the DiscordGateway's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the last time the operator synced with
                  Discord API
                format: date-time
                type: string
              maxConcurrency:
                description: MaxConcurrency is the max_concurrency value from Discord
                format: int32
                type: integer
              recommendedShards:
                description: RecommendedShards is the shard count recommended by Discord
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
